// packages/core/src/graphics/DrawOperation.ts

/**
 * Represents a drawable instruction that can appear on a PDF page.
 *
 * Concrete implementations include text, lines, rectangles,
 * images, and other graphical operations.
 */
 export abstract class DrawOperation {}